
<ul class="nav nav-pills mt-son-5">
    <div class="mx-auto d-flex">
        
  <li class="nav-item">
    <a class="nav-link active" href="#">Theo lộ trình</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="#">Phù hợp</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" href="#">Mới nhất</a>
  </li>
    </div>
</ul>

{{!-- khóa học in ra --}}

<div class="container">
    <div class="row mt-5">
        {{#each courses}}
        <div class="col-sm-4 col-sm-3">
            <div class="card">
                <a href="/courses/{{this.slug}}">
                <img class="card-img-top" src="{{this.image}}" alt="Card image cap">
                </a>
                <div class="card-body">
                    <a href="/courses/{{this.slug}}">
                    <h5 class="card-title">{{this.name}}</h5>
                    </a>
                    <p class="card-text">{{this.description}}</p>
                    <a href="#" class="btn btn-primary">Mua khóa học</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>
