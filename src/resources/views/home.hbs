        <!-- Masthead-->
        <header class="masthead_son">
            <div class="container px-4 px-lg-5 d-flex h-100 align-items-center justify-content-center">
                <div class="d-flex justify-content-center">
                    <div class="text-center">
                        <h1 class="mx-auto my-0 text-uppercase text-white">Học lập trình để đi làm</h1>
                        <h5 class="text-white mx-auto mt-4 mb-4">Trở thành lập trình viên nhà tuyển dụng luôn săn đón</h5>
                        <a class="btn btn-primary" href="/courses">HỌC NGAY</a>
                    </div>
                </div>
            </div>
        </header>


<div class="container">
    <div class="row mt-5">
        {{#each courses}}
        <div class="col-sm-4 col-sm-3 mb-4">
            <div class="card">
                <a href="/courses/{{this.slug}}">
                <img class="card-img-top" src="{{this.image}}" alt="Card image cap">
                </a>
                <div class="card-body">
                    <a href="/courses/{{this.slug}}">
                    <h5 class="card-title">{{this.name}}</h5>
                    </a>
                    <p class="card-text">{{this.description}}</p>
                    <a href="#" class="btn btn-primary">Mua khóa học</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>